Tenho uma VPS com uma rede caddy, e um sistema rodando instancias docker.
Para o economizze.shop, vamos criar um banco local mongodb e/ou um postgresql, via docker, para suprir a demanda desse site.

tudo sobre a infra da minha vps está em /home/viier/vps, os dockerfile e tudo mais.

Para imagens, usaremos o Cloudflare R2. fornecerei as credenciais futuramente.